docker exec postgres psql -U debezium -d testdb -c "
-- Создать тестовую таблицу
CREATE SCHEMA kafka
CREATE TABLE kafka.test_cdc (id SERIAL PRIMARY KEY, data TEXT);
INSERT INTO kafka.test_cdc (data) VALUES ('initial data');

-- Проверить
SELECT * FROM kafka.test_cdc;

"


docker exec -it kafka1 kafka-console-consumer \
  --bootstrap-server kafka1:9092 \
  --topic data.cdc.testdb \
  --from-beginning \
  --timeout-ms 10000



docker exec -it kafka1  kafka-topics --bootstrap-server kafka1:9092 --describe --topic data.cdc.testdb